# HANDOFF — actuele status & open punten (cold file)

> **Dit is de cold file van deze repo:** agents appen hier vrij — het zit NIET
> in de DSH system prompt, dus churn hier kost geen prompt-cache. De hot file
> (`AGENTS.md`) verandert alleen in gebatchte promotie-passes (werkwijze:
> `~/.dsh/AGENTS.md` → "AGENTS.md maintenance discipline"). Afgeronde sessies
> → `docs/ARCHIVED_HANDOFFS.md`. Verplaatst uit AGENTS.md op 2026-08-30
> (two-tier werkwijze).

### Model-mismatch contract gefixt (2026-09-01, GEDPLOYD — gate groen, restart 13:46 UTC, live E2E bewezen; zie ook de caretaker/handoff-sectie hieronder)

- **Incident (2026-09-01):** client vroeg lokaal model `qwen3.8-27b-uncensored-ymq`, backend draaide `gemma-4-E4B-it-uncensored`; caretaker `/ensure` gaf 200, de post-ensure-verificatie loggde alleen MODEL MISMATCH, en het verzoek werd alsnog doorgestuurd → **HTTP 200 door het verkeerde model** (stille substitutie). Launcher-oorzaak elders gefixt; dit is de gateway-side contract-fix.
- **Nieuw contract:** ná een switch/ensure-poging voor het opgevraagde lokale model verifieert de gateway vóór forwarding dat de live backend dat model dient — zo niet → **HTTP 503 `model_switch_failed`** op élk lokaal entry-pad: `/v1/chat/completions`+`/v1/completions`(+`/v1/messages`) krijgen de exacte OpenAI-body `{"error":{"message":"Model '<requested>' failed to load; backend serves '<actual>'","type":"model_switch_failed","code":"model_switch_failed"}}`; Ollama `/api/chat`+`/api/generate` krijgen de bestaande lokale-foutvorm (`detail.error=model_switch_failed`). Same-model cold-reload (requested == loaded) werkt onveranderd; cloud routing, failover-ordering en de vision-fallback (bewuste andere-model-route) zijn onaangetast; allowlist-geblokkeerd forward blijft bestaand gelogd beleid.
- **Wiring:** `engine/manager.py` → nieuwe live-probe `backend_serving_model_name()` (pgrep→gguf→reverse-lookup; `None` = niet probeerbaar (remote-F6) → géén mismatch, geen false 503's); `gateway/caretaker_runtime.py` → `verify_requested_model_served()` (alias-canonieke post-ensure-check); `gateway/routing.py` + `local_inference/ollama.py` → vlag `_switch_ensure_verified_pending` ná geslaagde ensure (reload-blok alleen bij `reload_model == requested`, switch-blok altijd), verificatie ná lock-release vóór forwarding. Verificatie zit ná `active_requests += 1` → finally-release blijft gebalanceerd.
- **Verificatie:** nieuw `tests/unit/test_model_switch_verification.py` (13 tests, incl. "niet doorgestuurd"-sentinel); de mismatch-pins falen bewust op de oude code (stash-bewijs: oude gedrag stuurde écht door). Suite: 1314/1 en 1313/2 (timing- + live-11434-flakes, beide in isolatie én op clean main groen — zie journal); ruff clean; py_compile OK. **Gedeployed:** pre-restart-gate 1315 passed / 3 skipped (13:25 min, idle box), push `c2ed720..ba9467e`, restart 13:46:54 UTC (startup adopteerde qwen schoon, geen forced switch). Voltekst → `docs/AGENT_JOURNAL.md` §2026-09-01.

### G3 bare-name routing hijack GEFIXT (2026-09-02, live bewezen) — uit pr-piet bugreport v3

- **Was:** bare `z-ai/glm-5.3-flash` resolved naar NVIDIA (404) i.p.v. openrouter — alle pr-piet tier-1 review-calls faalden (litellm stopt openai/, client kan niet prefixen). Oorzaak: `z-ai/`-prefix op nvidia + directory-volgorde (nvidia vóór openrouter) + eerste-prefix-wint-resolutie; failover probeerde niet door.
- **Fix (`7d5d32f`, gedeployed 16:13 UTC):** catalog-gestuurde disambiguatie via `set_catalog_probe()`-DI (positief catalog-bewijs breekt ties, nooit smallen naar None; exact-entries winnen; back-compat declaratievolgorde zonder bewijs) + `z-ai/` uit nvidia-prefixes. Gate 1324/3; live: bare name → 200 `"provider":"Z.AI"` in 2,5 s. Voltekst → `docs/AGENT_JOURNAL.md` §2026-09-02.
- **Bugreport-dossiers AFGESLOTEN (2026-09-02):** G2 disconnect-propagatie is gebouwd en live bewezen (zie G2-entry); de volledige C-feedback (C1–C11, incl. de v2-JSON in `scratch/`) was al afgehandeld door **PR #17** (`ef483dd`) met per-item verdicts — C2/C7 bleken (grotendeels) onterecht met refutatie (0 float-tokens in 214 records; `request_parameters` staat al op request_received), C8 (`streamed_ingress`/`streamed_upstream`), C9/C10 (`scripts/capture_query.py` + `--rollup daily`) en C11 (retention zichtbaar in `guardianctl status`; provider op cloud-records) zijn live. Onafhankelijk herverifieerd 2026-09-02.

### 2026-09-01 — Split-brain backend-launcher + /ensure-verificatie gefixt (caretaker-repo + host-unit; live bewezen)

- **Root-cause 1 — args-split-brain:** `llama-server.service` ExecStart wees nog naar de legacy v1-launcher (`/home/flip/llama_cpp_guardian/scripts/start_llama.sh` → leest `/home/flip/llama_cpp_guardian/config/current_model.args`), terwijl de caretaker z'n args schrijft naar `/home/flip/caretaker-llamacpp/config/current_model.args` (paths.py-topologie). Elke caretaker-(her)start lanceerde het model uit het STALE legacy-bestand → de eerste echte cross-model switch sinds de cut-over (11:49 qwen) faalde; gemma→gemma werkte per toeval, dus idle-unload/auto-reload leken gezond.
- **Root-cause 2 — /ensure false positive:** caretaker's post-load-verificatie was warn-only/fail-open; /ensure gaf 200 "confirmed" (11:51:41) terwijl /props het verkeerde model toonde (eigen supervisie zag de mismatch wél, 11:49:45, maar corrigeerde niet).
- **Fixes (m0nklabs/caretaker-llamacpp, branch `f6-phase-e-windows-process`, gepusht, service herstart 13:11 UTC):** `34dec2b` caretaker-owned `scripts/start_llama.sh` + unit ExecStart/WorkingDirectory daarnaartoe (launch-path uit de legacy-dir — F7-regel; binary-drop-ins intact); `e97496f` `/ensure` verifieert via GET /props vóór 200 (strict/fail-closed), mismatch → exact 1 retry → 503 `{"error":"model_mismatch","expected","actual","model"}`, no-op-path verifieert ook; suite 101 passed (10 nieuw), ruff clean.
- **Live bewijs (productie):** qwen3.8-27b-uncensored-ymq laadde 12:31:56 via caretaker-args (de exacte failing case van de operator; VRAM 11,1/12,3 + 15,3/16,3 GB); no-op /ensure verify 200 in 0,112 s; E2E via de gateway beide richtingen: qwen→gemma (modintel-key, 200 "OK" op échte gemma, 13:50:51) én gemma→qwen (agent31-verkeer, backend verified qwen). Tijdens de switch zijn de gateway's /ensure-client-timeouts (5 s) korter dan de echte reload → "transport error"-ERROR-logs + adopt-poll herstellen netjes (cosmetische logruis, bounded; poll-vensters zijn config-tunable uit PR #14).
- **Gedrag om te weten:** na een caretaker-restart is de in-memory `current_model` leeg → de eerste /ensure doet een volledige garantie-cyclus (~30 s bij qwen), óók als het juiste model al draait (conservatief-correct; adoptie-uit-/props op koude state = mogelijke optimalisatie). Handmatige `systemctl restart llama-server` is nu inherent racy met de caretaker-controller — liever via de gateway/caretaker schakelen.
- **Nieuwe open punten (update 2026-09-02):** ~~PR voor de caretaker-branch~~ **GEREGELD** — PR #9 (launcher + strict /ensure, ge-cherry-pick op schone branch vanaf actuele main; de F6-commits zaten al in main via squash #8) én PR #10 (latent-items: alias-resolutie in `switch_model` via bounded walk + truthful failed-switch bookkeeping: `_loaded_at`/skip-flag clear) doorliepen beide de volledige review-cyclus (PR #9: 2 terechte bevindingen gefixt met tests — realpath-patvergelijking + heal-save-skip) en zijn **gemerged** (`ba866ff`/`f0bdeb6`); productie-checkout draait main, caretaker herstart, live: /ensure met alias → 200 in 0,08 s. Agent31-verkeer (wisselend qwen/gemma) bleek client-zijde goed te zitten; timezone: dashboard toont UTC+2 (CEST), host/journal is `Etc/UTC`.

> Afgeronde/gesloten DSH-sessies worden gearchiveerd in **`docs/ARCHIVED_HANDOFFS.md`**
> (volledige tekst blijft bewaard; de Active Handoff hieronder houdt alleen sessies
> met lopende relevantie). Gearchiveerd op 2026-08-26 (2 batches):
> **Batch 1:** `20260826_muse_catalog`, `20260826_raw_capture`, `20260824_reasoning_effort`.
> **Batch 2 (alles afgerond):** `20260822_cleanup`, `20260822_nvidia_free_filter`,
> `20260824_cloud_audit`, `20260821_config_schema`, `20260815_bench`, `20260815_gcd`,
> `20260815_1`, `20260816_1`, `20260816_2`, `20260819_1`, `20260820_1`,
> `20260820_cloud_refactor`, `20260813_1`, `20260812_1`, `20260809_5`.
> Alleen `20260826_rename` blijft actief (huidige workspace-transitie) — de écht
> openstaande zaken staan hieronder in **Open punten**.

### 2026-09-01 (avond) — G2 orphan-calls gefixt (3-lagige root cause) + adopt-only eraf

- **G2 gefixt, gedeployed, live bewezen** (`3fa1479`/`f2d4d9f`/`6db7f5b`): non-stream cloud calls worden nu gecanceld zodra de downstream client verdwijnt. Live-verificatie: client SIGKILL op +8s → abort-log **binnen dezelfde seconde**, capture `request_cancelled`/`client_disconnect`/7,9s, **0 tokens** verbrand (vóór: 40–64s orphans met de volle output). Fix: forwarding non-stream racet de upstream-task tegen een disconnect-watcher (`asyncio.wait` FIRST_COMPLETED) → cancel + 499-contract + health-straf ontweken (`_CloudClientDisconnected` buiten de failover-except).
- **Root cause was 3 lagen diep** (zie JOURNAL): (1) cloud-routes bypassen de inference queue → de lokale disconnect-watcher draait er nooit; (2) de is_disconnected-polling-watcher vuurde in productie **nooit** (0 fires in een week — ook het lokale non-stream pad had dus een latente orphan-bug); (3) óórzaak van (2): de `track_api_usage_middleware` is een BaseHTTPMiddleware — mini-tests bewijzen dat is_disconnected-polling met die middleware nooit vuurt. **Fix: watchers consumeren de raw ASGI receive-channel** (werkt wél door BaseHTTPMiddleware; mini-test-bewezen). Beide watchers (cloud + queue) nu receive-gebaseerd.
- **Residual (gedocumenteerd, bewust niet gebouwd):** cloud-streaming detecteert disconnects pas bij de eerste chunk-write (pre-first-chunk gap); streaming orphans kunnen niet minutenlang doordraaien omdat writes falen zodra er gestreamd wordt.
- **GUARDIAN_STARTUP_ADOPT_ONLY=1 verwijderd** uit beide unit-files (`sed` + daemon-reload): gateway-sig was current (qwen3.8-27b-uncensored-ymq) en de boot-log bevestigt de normale signature-detectie. Startup-heal is weer volledig actief.
- **UNIT-VALKUIL (belangrijk):** `llama-guardian.service` is GEEN systemd-alias maar een **aparte unit-file** (disabled; eigen drop-ins). Een `systemctl restart llama-guardian` start een TWEEDE gateway die bind-conflicts geeft. Incident 21:12: mijn restart van de juiste unit verloor de poort-race van de stray → 28× crash-loop op guardian-llmprovider-gateway terwijl de stray het verkeer bediende (zelfde checkout/code, dus geen verkeersschade). Opgelost: stray gestopt, `disabled` en `Restart=no` in zijn unit-file. **De juiste productie-unit is en blijft `guardian-llmprovider-gateway.service`.** Open voor de operator: de llama-guardian.service-file verwijderen/hernoemen of laten (nu inactief en ongevaarlijk).
- Tests: 3 nieuwe cloud-forwarding pins + contract-test `test_begin_queued_request_cleans_up_waiter_on_disconnect` overgezet naar het receive-contract. Pre-restart gate: alle poorten PASS (eerste gate-run faalde op het oude polling-contract — exitcode checken met PIPESTATUS, niet via `| tail`).
- DSH-sessie-breuk 20:31 was NIET Guardian (gateway draaide door; de DSH-webserver op :3080 herstartte zelf).


## Open punten (actueel — alles wat hier niet staat is afgerond; details in `docs/ARCHIVED_HANDOFFS.md`)

- **Test-isolatie GEFIXT (2026-09-02):** `pyproject addopts = -m "not integration and not finetune_v2_live"` — de 20 live-tests (tests/integration/*, rakten productie :11434) zijn default gedeselecteerd; opt-in via `-m integration` / `-m finetune_v2_live`. Suite 1310 passed in 47 s; gate raakt productie nooit meer.
- **~~Bekende test-fout~~ opgehelderd (2026-09-02):** `test_cloud_attempts_resolve_google_full_address` is NIET reproduceerbaar op huidige HEAD — groen in elke suite-run vandaag (1310 passed ×4), groen geïsoleerd mét disk-cache weg (koude-conditie-simulatie), en Python CI is groen op een koude checkout. De weken-oude note was verouderd (de faal-conditie bestaat niet meer of was omgevingsgebonden); mocht hij terugkomen: eerst de disk-cache-weg-variant draaien.
- **PR #18 (G2-cap, andere agent):** disconnect-propagation is nu GEBOUWD en live bewezen (2026-09-01 avond, zie hierboven); over uit dat voorstel: streaming-teardown-test als échte pin en de bewust niet-geharde cap. Streaming pre-first-chunk gap is een gedocumenteerd residual (seconden-groot, bewust niet gebouwd — regression-risk > opbrengst).
- **Gateway restart-race GEFIXT (2026-09-02 laat, `ec1211e`):** wortel was niet de pid-file maar de listener-herkenning — `is_guardian_uvicorn_listener` eiste process_name=='uvicorn' + 'app.proxy.server:app' + '--port' in de cmdline, terwijl de unit `python3.14 -m app.main` draait (poort uit env): de stale-termination was dode code en een poort-houdende orphan overleefde 44 generaties. Herkenning nu op repo-root + app-module in beide opstartvormen; zelf-kill-guard en vreemde-proces-uitsluiting gepind. Post-restart-verificatie (MainPID == :11435-listener) is nu standaard-procedure. Open restant: de HERKOMST van de orphan (PPID=1, buiten cgroup, exacte spawner onbekend — CI-runner-hypothese; de kill-loop zelf is ermee gedempt).
- **ensure_fresh-wiring GEBouWD (2026-09-02 laat, `7f777a5`):** `/v1/models` triggert één fire-and-forget `ensure_all_fresh()` (no-op onder warme TTL, dedup-inflight, fail-open); `/ensure` transport error nu WARNING (adopt-poll-herstel is normaal gedrag). Live: /v1/models 200 met 272 modellen.
- **NVIDIA context-metadata gap (FUTURE):** NVIDIA-modellen krijgen de 131072-fallback; per-model `context_window`-overrides nog niet ingevuld (operator: "first find what's usable, THEN max context").
- **pi-modellen OPGESCHOOND (2026-09-02):** `~/.pi/agent/models.json` 216 → 99 entries, alle resolvem live (legacy `guardian/`-prefixes weg, bare-name cloudnamen → full addresses, dode lokale aliassen gedropt). Backup: `models.json.bak-20260902`. Alias-fix: `llama-guardian.service` is nu een **symlink** naar `guardian-llmprovider-gateway.service` (de aparte unit-file + drop-ins bewaard als `.disabled-20260902`) — `systemctl restart llama-guardian` is weer veilig, de repo-docs-belofte klopt eindelijk.
- **OpenRouter modaliteiten + catalogusvelden (GEWENST, deferred — operator-besluit):** `/models`-payload bevat `architecture.*_modalities`, `pricing`, `supported_parameters` e.d. die Guardian weggooit — nodig voor capability-gebaseerde vision-routing; dezelfde catalog-URL wordt 2× onafhankelijk gefetcht (consolidatie hoort in die verbouwing). Analyse → JOURNAL-archief §2026-08-30 catalogusvelden.
- **CI-adoptie (open sinds 20260813_1):** `scripts/pre_restart_check.py` als GitHub Action nog niet opgepakt.
- **llama-guardian.service file (operator-beslissing):** aparte unit-file (géén alias!), nu disabled + failed + `Restart=no`; verwijderen/hernoemen of laten — hij is inactief en ongevaarlijk, maar de naam in AGENTS.md-documentatie ("alias") is misleidend.
- **m0nkdash-origin (optioneel):** origineel achter `dashboard.oelala.xyz` blijft dood — raakt Guardian niet.
- **72h soak (passief):** capture-subsystem + de G1/G2/G3-fixes draaien; soak-window loopt.
