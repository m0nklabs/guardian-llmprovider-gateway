# AGENT_JOURNAL — append-only findings-log (cold file)

> **Werkwijze:** feiten/lessen die je had moeten opgraven (reverse-engineering,
> live tests, verborgen gedrag) → hier APPEN, zelfde sessie, met datum-kop.
> Dit bestand zit NIET in de DSH system prompt → appen kost geen prompt-cache.
> De **promotie-pass** (gebatcht, zie `~/.dsh/AGENTS.md` → "AGENTS.md maintenance
> discipline") distilleert periodiek: universele feiten → regels in `AGENTS.md`;
> detail → docs/-pagina's; verouderd → wissen of archiveren.
> Nederlands is prima (operator-facing, intern).

## 2026-08-30 — DSH zoek-tool kwaliteitsonderzoek + fixes

- Native `web_search` (deepseek-official) was kapot (geen key); na operator-key weer aangezet door andere agent, maar **BETAALD**: ~10.6k in + ~0.8k out tokens per call → rankt **#4** in de zoek-ranking, gratis engines eerst (Google-relay → SearXNG). Kwaliteit is prima (live: v0.3.0-releasepagina als #1).
- Ranking verankerd op 3 plekken: `~/.dsh/plugins/search-tool-ranking.js` (system-prompt), `~/.dsh/AGENTS.md`, repo-`AGENTS.md`-bullet.
- Local-search gateway-defects gefixt (`/home/flip/local-search` `gateway/app.py` +296/−42): Atom-RSS (GitHub-feeds gaven stil 0 items), `fetch_sitemap` expliciete 404, `map_site` echte root-linkdiscovery (Firecrawl /v2/map volgt alleen robots/sitemap), `fetch_meta` json_ld-cap 8 KB. Live end-to-end geverifieerd.
- DSH-bridge patch klaar: `dsh-mcp-client/lib/index.js` resource-pass-through (fixt `get_file_contents` "[resource: content discarded]") — **actief na DSH-herstart**; node_modules-patch gaat verloren bij een dsh-update → opnieuw toepassen.
- **Les:** DSH-plugin `disabled: true` verwijdert een plugin niet altijd — `tool-web` stond disabled in de dump maar de tool bleef geregistreerd. Verifieer met `dsh --profile <p> --dump-config` én de live tool-lijst, niet alleen de dump.
- Klein restpuntje: `fetch_meta` negeert `include_links: false` (links worden altijd meegeleverd) — cosmetisch, op te pakken in `mcp-server/index.js`.

## 2026-08-30 — Two-tier AGENTS.md-werkwijze ingevoerd

- Active Handoff + Open punten verhuisd naar `docs/HANDOFF.md` (cold, 8.5 kB); AGENTS.md = stabiele regels + pointers (28.9 kB), verandert alleen in gebatchte promotie-passes.
- Reden: elke byte-verandering in de hot file maakt de prompt-cache-prefix ongeldig; churn hoort in cold files (gratis), optimalisatie in batches (1× cache-invalidation per pass).
- Guard-plugin `~/.dsh/plugins/agents-md-guard.js` bewaakt hot- én cold-drempels (config: `cordis.patch.yml`).

## 2026-08-30 — F5-tranche-2 merge + caretaker-contract-PR (sessie-fortzetting)

- **PR #12 gemerged** (operator, 10:39:01Z, squash `1402b10`); gateway main nu op 1134 tests. De remote-first hotpath is actief zodra de daemon het contract shipt — de gateway valideert `supports_fresh_load` op ELKE 200 (`"fresh_load" in value`), dus geen gateway-side versie-afhankelijkheid: het contract activeren = alleen de daemon upgraden.
- **Caretaker-contract-PR #7 (merge-klaar, wacht op human merge):** `switch_model` → bool `fresh_load`; `/ensure`-200 antwoordt `{ok, loaded_model, fresh_load, vision_enabled, needs_reload}`. Twee review-rondes, beide terecht-in-de-geest:
  - r1 (taerecht): de no-op fast-path rapporteerde "already active" zonder backend-health — een crash tussen watchdog-ticks laat `current_model` gezet achter met een DODE backend → /ensure lag ("ok: true, fresh_load: false" op een dode backend). Fix: no-op-gate eist geslaagde `server_process.health_ok(...)`; dode backend → geen no-op, healing-reload (fresh_load=True). Pin: `test_switch_model_noop_refused_when_backend_dead`.
  - r2 (deels terecht): de claim "TypeError bij health_ok() zonder URL" klopte NIET (de enige definitie heeft een default-arg; geen subklasse-overrides), maar de bare default gebruikte de IMPORT-tijd `SERVER_URL`-const terwijl de flow bewust call-time `self.server_url` gebruikt → gefixt naar `health_ok(self.server_url)`. De concrete review-suggestie (`health_ok(f"{server_url()}/health")`) zou een dubbel `/health`-pad bouwen — health_ok plakt zelf /health aan (manager.py:115); call-sites geven de basis-URL door. Les: bij review-suggesties die een URL construeren, check eerst wat de callee zelf met de parameter doet.
- **Caretaker-suite 84 passed (2× stabiel); 1 flaky geobserveerd** (`test_switch_model_swap_frees_old_slot_without_deadlock` faalde 1× in de volle suite, slaagde 3× daarna incl. isolatie) — waarschijnlijk machine-timing (wait_for timeout=2); volgen.
- **Oud stash-conflict in caretaker-AGENTS.md opgelost:** commit `089fa38` had de conflict-markers (`<<<<<<< Updated upstream` / `>>>>>>> Stashed changes`) vastgeschreven; de verouderde "Updated upstream"-variant (Phase A "niet gecommit") verwijderd, de actuele variant behouden.
- **Les (2× bevestigd deze sessie):** stash-pop na branch-switch: tracked en untracked bestanden apart stashen (`git stash push -- <file>` vs `git stash push -u`), anders vallen de untracked files buiten de eerste push.

- **Review-cyclus-les (2026-08-30, opgepikt door de operator):** de merge-klaar-check gebeurde vóór 11:31 terwijl de diepe rerun om 11:31:02 nog een incremental review MET een open thread postte (caretaker PR #7) — "0 threads" op t=11:26 was al stale bij het merge-signaal. Werkwijze voortaan: **thread-check herhalen vlak vóór het merge-signaal**, en de laatste review-body (key-observations/incremental) volledig lezen vóór het slot-comment — een "Possible Issue (Not Verified)" kan als review-thread landen óók als de merge-state CLEAN blijft (zonder branch-protection tellen open threads niet als BLOCKED). Slot-comment na de laatste review is verplicht onderdeel van het merge-signaal: de operator moet zien dat de laatste review verwerkt is (beantwoord/deferred met bewijs), niet alleen dat de checks groen zijn.
- **Caretaker PR #7 r3 (incremental 11:31:02) verwerkt als deferred trade-off:** de no-op-gate behandelt één gefaalde health-probe als dode backend. Weerlegging met ≥2 bewijzen: (1) `health_ok` abstraheert ConnectError (dood, direct refused) vs ReadTimeout (levend, >5s) weg naar bool — de retry raakt alleen het hypothetische levend-maar-traag-scenario; (2) kost-analyse: false-negative = één self-healing herstart + de gateway-restore is ná een échte herstart ontworpen gedrag ("stale context" klopt niet); false-positive (hangende backend als authoritative) = alle requests hangen tot client-timeout, watchdog ziet hangen niet. Fail-closed in de veilige richting bewust. Retry (2e probe na 0.5s) verzacht het zeldzame scenario maar verlengt de heal-detectie op de hangende path en voegt een magic delay toe. Follow-up alleen als productie-logs het scenario tonen.

- **Merge-status 2026-08-30 (middag):** caretaker PR #7 gemerged (squash `6925207`) — het ensure-response-contract is nu caretaker-main; gateway PR #13 gemerged (squash `ff8d6bc`); gateway PR #14 (poll-vensters) merge-klaar. Praktische les bij PR #7: `mergeable=null/state=unknown` kwam van de merge ZELF die tijdens het pollen doorging (GitHub stopt de mergeability-berekening op een gesloten PR) — check eerst `origin/main` vóór je een glitch vermoedt.
- **PR #14 review-cyclus (3 rondes, alle terecht):** r1 onafhankelijke bounds → 60×30 s = 30 min worst-case (gecombined budget-cap); r2 de cap telde alleen de sleep-delen — elke re-bind-iteratie await óók een volledige /ensure (tot client_timeout) → per-attempt-divisor; r3 de 2 initiële probes draaien óók onder de lock (totale worst-case claim) én cap=0 schakelde het re-bind-herstel stil uit bij hoge timeouts → ceiling 30 s → cap ≥2. Les: bij "budget"-claims in lock-dragende paden, inventarise ALLE bijdragen aan de lock-hold (probes, round-trips, sleeps) vóór je de cap-formule schrijft — en test de in-bounds extreme hoeken, niet alleen de defaults.

- **Heuristiek-opruim audit (na PR #7-merge):** de daemon-geautoriseerde `vision_enabled` is al primair op de remote-path (PR #12 bouwde `daemon_vision if isinstance(bool) else probe` — de probe alleen als fallback voor oudere daemons). Resterende `current_runtime_uses_mmproj`-plekken zijn NOODZAKELIJK, niet heuristiek: de outage-adopt-path (caretaker_runtime:743/752 — géén /ensure-response beschikbaar bij een outage, de probe is de enige bron) en de routing auto-switch-lezing (routing:534/559 — lokale state vóór een switch-besluit, geen roundtrip). Enige restpost: de verouderde module-docstring in caretaker_runtime (regel ~55-62 beschrijft de probe nog als primair) — deferred naar de volgende code-touch (F6), geen review-cyclus waard voor een docstring.

## 2026-08-30 — avond: F6/F7-bouw afgerond (4 PR's merge-signaal), sessie-overdracht

- **Gebouwd deze sessie (deels via worker-hiërarchie, per operator-voorkeur):** gateway **#14** (poll-vensters → config, 3 review-rondes allemaal terecht), gateway **#15** (F6 Windows LAN-provider + managed-heuristiek: `local:`-marker overrulet de `-local`-suffix, quoted-boolean-normalisatie, onbekende tokens → suffix-fallback), gateway **#16** (F7-staging systemd-unit `guardian-llmprovider-gateway.service` + alias `llama-guardian.service` + FILE_REGISTER-delta 35 files), caretaker **#8** (Phase E `WindowsDirectServerProcess`: taskkill-tree-kill + bounded wait, `CREATE_NEW_PROCESS_GROUP` 0x200, non-POSIX args-splitter, NSSM-deploy-doc).
- **Caretaker #8 review-cyclus (ronden op `8310151`-lijn):** (1) taskkill-stop-race — TERECHT, bounded `proc.wait()` spiegelde de POSIX-branch (MSDN: TerminateProcess is async); (2) `CREATE_NEW_PROCESS_GROUP`-fallback + test-pin stonden als 0x400 (CREATE_UNICODE_ENVIRONMENT) i.p.v. 0x200 — MSDN + CPython `_winapi` + live-constcheck; (3) env-vars-dropped — WEERLEGD: `DirectServerProcess` merged `CURRENT_MODEL_ENV_FILE` ook niet (de reader is de systemd-wrapper; Windows pinned env via NSSM `AppEnvironmentExtra`); (4) taskkill-fail-silence — TERECHT (low severity): module logger + 2 warnings, geen control-flow-change (POSIX-branch swallt ook).
- **Les (herhaald én verscherpt — "houdbaarheid"):** een "clean"-oordeel op een PR heeft houdbaarheid. Nadat de worker #8 als klaar rapporteerde, kwamen nóg een open review-thread (single-quote, van de verse `/review` op de laatste commit) én een CI-fail binnen. Thread-check + CI-check zijn pas geldig op het moment van het merge-signaal zélf.
- **Weerlegging-premise-patroon:** de single-quote-bevinding veronderstelde dat de builder `shlex.quote` (single quotes) emit. Code-lezing: `_build_args_string` (caretaker/manager.py:578–581) bouwt een ongequote space-joined f-string; nergens `shlex.quote`. Bij review-bevindingen over "wat de builder emit": lees eerst de builder zélf — het idiome ("idiomatic quoting helper") is niet wat er daadwerkelijk gebeurt. Paths-met-spaties is een pre-existing beperking van het args-file-formaat (beide impls) — follow-up.
- **CI-fail-protocol bevestigd:** `test_switch_model_swap_frees_old_slot_without_deadlock` (TimeoutError) faalde in CI op de PR-head terwijl de lokale suite 91 passed was — en had al flake-historie vóór deze PR (zie de middag-observatie hierboven: 1× rood, 3× groen). Volgorde: lokaal ≥3× + pre-PR-base + run-historie → pas daarna `gh run rerun --failed`. Nooit een speculatieve fix pushen op een niet-gereproduceerde fail.
- **Suite-tellings-drift (3e keer):** PR-thread-tellings (1140/1141/1142/1144/1145/1146) zijn branch-basis-afhankelijk en geen waarheid op main. Volgende agent telt ZELF na de merges; verwachting gateway ~1144–1146, caretaker 91 (op `8310151`) — altijd verifiëren.
- **Overdracht:** de volledige sessie-state, merge-signalen en de route na overname staan bovenin `docs/HANDOFF.md` ("Sessie-overdracht 2026-08-30 avond").
- **Caretaker #8 eindstatus (avond): MERGE-KLAAR op head `d6ff3e2`.** De CI-fail was een **runner-contentie-flake by design**: de Phase-C swap-test praat tegen de échte llama-server op `127.0.0.1:11440` op de gedeelde GPU-runner — rerun-attempts 1–2 gaven identieke TimeoutError (runner-4/runner-2), attempt 3 groen (runner-1, 91 passed); lokaal 9× groen incl. de pre-PR-basis. Les: een "unit-test" die tegen een echte locale backend praat is op gedeelde runners een flake-by-design — de rerun-loterij is symptoombestrijding; de échte fix is een HTTP-mock-backend voor de Phase-C/D swap-tests (follow-up). De single-quote-thread: het premise-deel bleef weerlegd (builder emit nooit quotes), maar de divergence was reëel (POSIX-shlexer pelt operator-embedded single quotes af, de Windows-splitter niet) → defensieve both-styles-strip (`d6ff3e2`). Twee workers op dezelfde PR zonder conflict: de tweede verifieerde vóór elke actie wat de eerste al gedaan had ("verify, don't duplicate") i.p.v. blind de briefing uitvoeren — de briefing zei "geen code-wijziging", de live state was al voorbij dat punt.
- **PR-infra-les #1 (veld-geproven, caretaker PR #8):** een auto-`pull_request`-run die <1 min na start door de concurrency-cancel valt is door GitHub **niet meer re-runbaar** ("cannot be rerun; workflow file may be broken" — de run was geannuleerd vóór de workflow-checkout, dus `gh run rerun` weigert). Werkende alternatief: **PR close+reopen** — de `reopened`-trigger staat in pr-piet.yml en levert een verse auto-run. Getest: run 33331954331 dood → close+reopen → run 33332549005 success → merge-state CLEAN.
- **PR-infra-les #3 (caretaker PR #8):** pr-piet triggert op élke `issue_comment` (created én edited), maar behandelt non-slash-comments als "Unknown command" → élke slot-comment levert rode "comment"-runs (tier-1 failure exit 3, tier-2 skipped) ZONDER review, ZONDER threads, ZONDER merge-state-impact — comment-runs hangen niet aan de head-rollup. Rode comment-runs na een slot-comment zijn dus cosmetisch; reviews-of-record zijn de geslaagde `pull_request`-runs. Cross-repo-journal-les erbij: het flake-bewijs stond in de gateway-journal (regel 32, PR #7-cyclus) en werd pas de 3e bewijslijn toen een worker er expliciet om vroeg — **lees bij flake-diagnoses óók het journal van de zuster-repo** (caretaker- en gateway-historie overlappen via de gedeelde infra).

## 2026-08-30 — capture-feedback C1–C11 verwerkt (branch `f-capture-feedback-c1-c11`)

- **Aanleiding:** rapport van de pr-piet maintainer agent na de eerste echte end-to-end analyse op `data/capture`. Alle 11 items geverifieerd tegen code + live records vóór implementatie (3 fresh-context workers, strikte file-ownership, lead-integratie).
- **Geïmplementeerd (schema 1.1.0, additief):** `started_at_utc`/`completed_at_utc` (C1); `finish_reason` áltijd aanwezig + `native_finish_reason` (C4 — live: 248/248 cloud completed-records misten hem); usage-mirror `completion_tokens_details` as-is + `native_tokens_reasoning`/`native_tokens_cached`/`cost`/`provider_name` + `app_title`/`app_referer` (C5); `caller_request_id` via `capture.correlation_headers` (C6); `streamed_ingress`/`streamed_upstream` (C8); `provider` per poging op cloud-events (C11); actieve capture-file plain `.jsonl` + gzip-on-rotate (C3 — terug naar het originele Keanu-plan, deels omkering operator-besluit 2026-08-26, bij akkoord getoetst); `scripts/capture_query.py` (filters + `--rollup daily`, C9/C10) + `docs/schema.md` + guardianctl-retentie-zichtbaarheid.
- **Gerefuteerd (live-bewijs):** C2 — 0 float-tokens in enig record, de "131072.0" stond alleen ín berichtinhoud; C7 — `request_parameters` op `request_received` bevat al max_tokens/temperature/stream/tools. C8-waarneming ook niet reproduceerbaar. Alle drie gedocumenteerd in `docs/schema.md`.
- **Nieuwe bugs onderweg gevonden + gefixt:** NEW-4 — null-usage liet via fail-open geslikte AttributeError héle `request_completed`-events wegvallen (verklaart waarschijnlijk deels "missing usage" in productie); NEW-2 — streaming-completions met `attempts: 0`; latent lokaal pad las `finish_reason` uit message i.p.v. `choices[0]`.
- **Lessen:** (1) DSH-goal-rounds ≈ 1–3 min — worker-voortgang check je op /tmp-scratch + git-status, niet op "nog geen repo-edits". (2) Cross-worker-risicopunten (gedeeld test-bestand met hardcoded layout-naam; init-wiring in server.py) vang je af met ownership-tabel + vroege coordination-note — beide kwamen tot bloei. (3) Consumer-verdachten zijn hypotheses: 2 van 11 onterecht; de eigen vooraf-steekproef (960 events) gaf exact dezelfde refutaties als de worker-rapporten.
- **Open:** PR wacht op /review + human merge; code pas live na restart (operator-run) en tot F7 draait productie uit de legacy-dir (backport = aparte beslissing). Journal-budget naar 20000 verhoogd in `cordis.patch.yml` (operator: 10000 te laag).
- **PR #17 review-ronde 1 afgehandeld (deep review 22:05Z, CHANGES_REQUESTED):** 3 bevindingen, alle terecht → gefixt: (1) `capture.correlation_headers: []` werd stilletjes door de default vervangen (`normalized or DEFAULT`) — explicit-empty is nu een échte opt-out (all-invalid lijst valt nog wél terug op default; 3 tests), door de reviewer als security-concern gemarkeerd; (2) `--limit 0`-off-by-one in `_limited` van `capture_query.py` (yield vóór de limit-check → rollup aggregeerde 1 record) — check-before-yield + 2 tests; (3) dubbele `TestSharedCrossRepoVector` in test_capture_schema.py — git-diff bewees dat de PR de kopie aan de staart toevoegde (de reviewer beschreef de positie omgekeerd; origineel op main's plek behouden). Suite **1266 passed / 3 skipped**. **Les:** een shadowing-dubbelklasse valt NIET op via pytest (de zombie draait gewoon niet) — alleen diff-inspectie vangt het; worker-A's 477-regel hunk voegde ze per ongeluk mee. **Nieuwe hot-rule (operator-verzoek, in AGENTS.md):** een merge-gereed verklaarde PR krijgt minstens één slot-comment dat aangeeft dat de laatste review bewijst dat de issues met overwegingen zijn beoordeeld.
- **Review-ronde 2/3 (heads 4e47a4b→6864796→ff66ec9):** (1) CI-ruff F841 in de nieuwe wal_writer-tests (unused `legacy` → nu meaningful migratie-assertie); (2) deep review vond een écht defect in de herschreven `_enforce_retention`: age-loop liet verwijderde entries in `units` staan → quota-loop popte ze opnieuw (no-op `_remove`, maar `total_size -= size` dubbel) → vroeg uit met echte files nog over quota; survivor-aanpak + regressietest; (3) deep review vond de tool-call-waste-fout in `capture_query.py`: pure tool-call-completions (lege content, wél tool_calls = normaal agent-verkeer) telden als waste → nu uitgesloten + P9-fixture + rollup-pins. **Infra-les:** het synchronize-event voor head 6864796 leverde 0 check-runs op (close+reopen triggerde alleen pr-piet, niet CI) — de bewezen hamer is een lege commit pushen; daarna verschenen alle 15 check-runs. Suite **1267 passed / 3 skipped**.
