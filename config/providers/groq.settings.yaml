# Groq cloud provider.
enabled: true
base_url: https://api.groq.com/openai/v1
api_key: ${GROQ_API_KEY}
timeout_seconds: 1200
# Hard cap (seconds) on ONE upstream call attempt incl. 429 backoff sleeps (G2).
max_call_seconds: 1200
